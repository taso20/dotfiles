# --- プラグインマネージャ (lazy.nvim) ---
# プラグインのソースコード自体は GitHub から落としてくるので不要です
/lazy-repo.json

# ※ lazy-lock.json は含める派と含めない派がいます。
# 他のPCで完全に同じ環境を再現したいなら、あえて書かずにGitHubに上げてください。
# もし「設定ファイルだけでいい」なら、下の行の # を消してください。
# /lazy-lock.json

# --- LSP / ツール管理 (mason.nvim) ---
# インストールされた言語サーバーやデバッガの実体（バイナリ）はOS依存なので除外します
/mason/

# --- 一時ファイル・キャッシュ ---
# Neovimが自動で作るバックアップや履歴ファイル
*.swp
*.swo
/.netrwhist
/undo/
/shada/

# --- ログファイル ---
*.log

# --- OS・エディタ固有のファイル ---
.DS_Store
.directory
